# dev 模式专属（vite dev = development 时加载；生产构建不加载此文件）
# 直连本地后端：浏览器直接请求 http://127.0.0.1:8001，不再经 Vite proxy 同源转发
# 依赖 backend/.env 的 CORS_ORIGINS 放行前端 origin（默认已含 http://localhost:5173）
# 注意：dev server 端口被占用会顺延（5174…），需把对应 origin 加进 CORS_ORIGINS
# 注意：若临时开 VITE_USE_MOCK=true，绝对地址会绕过内存 mock（mock 只拦同源 /api），
#       届时注释掉下面的直连即可回到同源 + mock
VITE_API_BASE_URL=http://127.0.0.1:8001
