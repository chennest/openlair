# 2026-09-05

## laircli skill 分发到用户级目录

- 源头：`D:\002-code\openlair\laircli\skill\`（SKILL.md + references/commands.md，agent_created laircli skill，随 laircli 代码演进）。
- 已复制到两处分发副本：
  - `D:\001-code\笔记\.agents\skills\laircli\`（笔记库 .agents 约定目录，供遵循该约定的 agent 触发）
  - `C:\Users\chennest\.workbuddy\skills\laircli\`（WorkBuddy 用户级 skill 目录，任何会话可自动发现）
- 同步策略：laircli 命令面变更时，从仓库源头重新复制一次。
- lair 链路验证：二进制在 `C:\Users\chennest\.vfox\sdks\golang\packages\bin\lair.exe`（PATH 可用），配置 `~/.laircli/config.json`，`lair whoami` 连生产正常。

## 记账页布局重构（mock 实测通过，commit 35cbe37 @ test）

- 需求来源：用户先看 AI 生成/编辑的效果图对齐方向（gpt-image-2 images edit 基于真实截图编辑），定稿「顶部 slim 摘要条 + 流水通栏」骨架。
- 改动（`lairweb/src/modules/ledger/`）：
  - 新增 `LedgerStrip.vue`：支出/收入/结余/预算（内联调整）+ 近6月迷你趋势，单行面板
  - `LedgerTable.vue` 加 `toolbar` slot；`LedgerFilter.vue` 去卡片外壳收进流水卡头部
  - 删除 `LedgerBudget/LedgerTrend/LedgerStats.vue`（-413 行）
- 验证：`pnpm run build` 通过；`VITE_USE_MOCK=true pnpm run dev` + agent-browser 登录 mock 种子账号（test1@openlair.dev/test123456）截图确认。
- 排错记录：
  - Vite 启动报 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]`——沙箱批量删除保护拦截 `node_modules/.vite/deps` 清理；手动 `rm -rf node_modules/.vite` 后正常。
  - agent-browser 登录页输入框无 id，用 `snapshot -i` 的元素 ref（@e5/@e7）操作；`screenshot` 参数是 `--full <path>` 不是 `--full-page`。
- 注意：分类统计（categoryStats）这版从页面上撤掉了，API 仍在；用户要可后续加回（如弹窗/抽屉）。

### 追加：流水列表表格化（commit 1075214，已推 origin/gitlab test）

- 用户反馈通栏后行内容稀疏（badge+备注+金额之间大片空白），要求表格格式。
- `LedgerTable.vue` 行改 grid 栅格：`92px 分类 | 1fr 备注 | 120px 金额 | 36px 操作`，共享账本插 96px 记账人列；表头 `.thead` 与行共用列宽；金额右对齐；备注 ellipsis；≤680px 隐藏表头和记账人列。
- mock 实测截图：`.workbuddy/outputs/ledger-table-live.png`。

### 追加：流水升级为完整数据表格（commit aa770f1，已推 origin/gitlab test）

- 用户要求"完整的表格功能"：改用 shadcn Table（src/components/ui/table/），标准列 日期/分类/备注/记账人(共享)/金额/操作；去掉按日分组，日期独立成列（当年显示 MM-DD，跨年显示完整年份）。
- 完整分页器：每页条数 Select(10/20/50，切换回第1页) + 共N条·第x-y条 + 页码 x/y + 上下页；新 emit `page-size`，index.vue 映射 `handleQueryChange({pageSize, page:1})`。
- mock 实测截图：`.workbuddy/outputs/ledger-full-table.png`。

