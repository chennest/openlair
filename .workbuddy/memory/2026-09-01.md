# 2026-09-01

## 创建 laircli 用户级 skill
- 位置：`~/.workbuddy/skills/laircli/`（SKILL.md + references/commands.md），经 quick_validate 校验通过。
- 用途：指导 AI 代理通过 `lair` 命令（OpenLair Go 版 CLI，X-API-Key 鉴权）代用户记账/待办/日程/笔记/习惯/总览；含命令速查、关键契约（bookId 解析、category 名→id、type 中文枚举、code=200、422 裸响应、退出码）、安全边界（不做删账本/签发 Key）。
- 内容基于 docs/cli/cli-guide.md 提炼，references/commands.md 为自包含完整命令参考。
- 按用户要求移到项目级 `laircli/skill/`（随仓库提交，已 push test）；用户级副本已删。
- 补充记账 SOP：先 `lair book current` 确认账本 → `lair ledger categories` 拉分类 → `lair ledger add` 记录（分类/账本以实时返回为准，禁止凭记忆猜）。
- 提交 `1e868ad` 已推送 gitlab + origin。
- overview 增加最近 10 笔流水：后端 `OverviewService.get` 加 `recentLedger`（query_transactions 日期倒序取 10 笔 + 分类名映射，无账本返回 []）；CLI overviewDTO 加 RecentLedger、overview.go 用 render.Table 渲染「最近流水」区块；后端 test_overview_aggregates 与 CLI TestOverview/harness fake 同步；skill 文档更新。后端 24 测 + CLI go test/vet/gofmt 全过，提交 `3800dd7` 已推 gitlab + origin。
