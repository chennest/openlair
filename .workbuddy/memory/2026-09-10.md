# 2026-09-10

## 记账表格加编辑 + 记账人列常显（commit c4972b0，已推 origin/gitlab test）

- 需求：行内只有删除，缺编辑；「记账人」列原本只在共享账本显示。
- 改动（`lairweb/src/modules/ledger/`）：
  - `api.ts` 补 `update(id, patch)` → PUT /api/ledger/{id}（后端 + mock 早就有，只是前端没接）
  - `LedgerDialog.vue` 加 `transaction` 可选 prop → 传入即进入编辑模式（预填原值、标题「编辑流水」、按钮「保存修改」、emit `update` 事件）；同时修了一个老 bug：`saving` 标志从不复位导致二次打开弹窗卡在「保存中…」
  - `LedgerTable.vue` 行操作列加 Pencil 编辑按钮；记账人列去掉 `v-if="shared"` 常显
  - `index.vue` 新增 `editingTx` ref + `openEdit` / `closeDialog` / `handleUpdate`，弹窗通 create/update 双模式
- 验证：`pnpm run build` 通过；mock 实测 PUT /api/ledger/13 body={amount:499,...} 两次成功（dev server 日志确认）。

## 弹窗灰底根治 + 表格布局精修（commit a95506a，GitLab 已推，GitHub 未推——网络不通且超哥说不用急）

- **灰底根因**：`BaseModal.vue` 的 `.modal` 在 transition 结束态带 `backdrop-filter: blur(20px) saturate(180%)`，与 `.modal-mask` 的 `rgba(0,0,0,0.28)` 遮罩叠加，弹窗表面透出模糊深色层呈灰色。修复：移除弹窗表面玻璃层（回归"纯白+双层阴影"），`.modal-mask` 改 flex 居中 + `.modal` height:auto 防拉伸留白。
- **表格精修**：列宽 日期96/分类104/备注1fr/记账人128/金额140/操作96；表头与单元格 padding 对齐（首尾列 pl-5/pr-5 = 面板 20px）；行高统一 52px；表头降权 11.5px text-3；hover 换 var(--hover)；末行去重复边框。
- 排错记录：agent-browser 的 Chrome 起不来（exit 0 无 DevToolsActivePort，taskkill chrome 后仍复现），浏览器验证放弃，改为纯代码审查定位 + build 验证——超哥也明确说"不用啊，你自己看代码就好了"。

## 分类系统升级：seed 扩充 26 类 + 用户自定义分类（未提交，等超哥发话）

- 需求：16 个固定分类太少（缺数码/硬件等），拍板方案=先扩 seed 打底 + 开放用户自定义（删除保护=挂流水 409 拒绝；共享账本=系统预置+所有成员自定义共用）。
- 后端：
  - `models/category.py` 加可空 `user_id`（NULL=系统预置），Alembic 迁移 `a9c3e7d1b5f8`（head=b8c4d6e8f0a2）
  - `seed.py` 16→26（支出 18 + 收入 8，sort_order 支出块 0-17/收入块 18-25，「其他」保持兜底位，id 1-16 不动，新增 17-26）
  - `repositories/ledger.py`：categories 加 user_ids 过滤 + category CRUD/usage_count/exists
  - `services/ledger.py`：categories DTO 加 userId + 可见范围（bookId=账本成员共用/userId=本人）、create/rename/remove 分类（409 重名、403 系统预置、409 删除保护）
  - `endpoints/ledger.py`：GET /api/ledger/categories 加 bookId；POST/PUT/DELETE /api/ledger/categories
  - assistant 同步：`runtime.py` system prompt 喂分类、`tools/ledger.py` list_categories/execute_create_plan 匹配均改 user 维度
  - 测试：test_ledger_categories 断言更新 26 + 新增 test_category_crud_and_visibility；pytest 72 passed
- 测试库（6.28 openlair）：alembic upgrade head 已执行；INSERT 10 新分类 + UPDATE sort_order 重排（必须 `mysql --default-character-set=utf8mb4`，否则中文 Incorrect string value）
- 前端：`api.ts` Category.userId + categoryApi + categories(type,bookId)；`LedgerDialog.vue` 内联「管理分类」视图（不叠双层 modal，标签"系统/我的"，行内改名，409 错误展示）；`index.vue` loadCategories 带 bookId、切换账本/管理完成自动刷新；mock store seedCategories 26 个 + visibleCategories/categoryExists/createCategory 等仓储函数 + 四个分类端点 mock
- **顺手修 mock 既有 bug**：`update/remove` 流水 mock 用 `String(req.params.id)` 与 number id 严格比较恒 false——mock 模式下编辑静默失效、删除恒 404。改 Number 比较修复并回归验证（PUT 生效、DELETE 200）。其他模块（todo/calendar/notes/habits）的 :id mock 可能有同样模式，待排查。
- 排错：① alembic MultipleHeads——ls|tail 截断看漏 `4d8f9a0b1c2d_add_invite_code` 迁移，真实 head 是 b8c4d6e8f0a2，down_revision 写错导致 64 测试失败，修正后全过；② 5173 端口被 06:11 遗留 dev server（旧代码）占用，curl 打到旧代码得出假失败结论，PowerShell Get-NetTCPConnection 定位后清理重启才测准；③ mock HMR 重置 store 导致跨热重载的测试脚本计数错位（功能本身没问题）。
- laircli 零改动兼容（categoryDTO 未列字段解码忽略）。
## 09:29 MR#5 合并报错排查（小白）

- MR #5 (test→master) 两个问题：① test_business_api.py 合并冲突 ② pipeline #99 deploy-test 失败
- 冲突解决：master 侧 make_client 开放注册 + test_register_closed_by_default；test 侧 test_register_status_endpoint + 分类升级。保留两侧，注册 4 测试全过，merge commit 4010806 已 push，MR 变 mergeable
- deploy-test 失败真相：kubectl rollout status --timeout=5m 假阴性。pod 创建→Ready 实际 7m58s，其中拉镜像 7m36s（新 tag 首拉）、应用启动仅 22s。集群实际部署成功（test-99 已 Running）
- 每次新 pipeline 新 tag 都要重拉镜像 → deploy-test 大概率复现超时；候选修复：rollout timeout 5m→12m
- 本地 stash 了一条 "wip: ci job 默认超时 20m"（.gitlab-ci.yml default timeout），超哥明确说不要了，待丢弃
- 后续：pipeline #100 全绿（deploy-test 仅 44s，镜像分层缓存命中），MR#5 完全 mergeable；废弃 stash 已 drop。merge 动作等超哥发话
## 10:02 v0.2.0 发版（小白）

- MR#5 合并（glab 需要 -f sha=<源分支sha> 防竞态校验），merge commit bebebe3b
- 在 master 打注解 tag v0.2.0（laircli/分类系统/注册开关/CI 修复 四条注解）并 push
- tag 触发 pipeline #101：backend-test + backend-package + web-package 跑构建，deploy-prod manual blocked（job 456）
- 删除 test-deploy-001~005 五个测试 tag（远程+本地），现在仓库只剩 v0.1.0 / v0.2.0
- 提醒过：deploy-prod rollout timeout 也是 5m，HK 首拉 v0.2.0 镜像可能超时假阴性，放行后要盯
## 10:20 gitlab-runner pull_policy + 生产发版确认（小白）

- 超哥指路：remote-ssh skill 在 D:\001-code\笔记\.agents\skills\remote-ssh（SKILL.md 要求先读服务器清单+helm SOP，统一 agentsshcli 操作）。已读，后续远程操作必须走 agentsshcli + 服务器清单
- HK helm releases：gitlab-runner-prod（0.92.1）/ traefik / traefik-crd
- runner 自身 pod 默认 IfNotPresent；job pod 无 pull_policy → latest tag 每次 Always 拉。bitnami/kubectl 实证只有 latest + sha256 digest tag（Broadcom 收购后不发版本 tag）
- 已按 SOP 修复：values 固化 /code/helm/gitlab-runner-prod/values.yaml，[runners.kubernetes] 加 pull_policy=["if-not-present"]，helm upgrade --version 0.92.1（REVISION 2），configmap 生效，runner pod 重启正常
- deploy-prod(job 457) 被放行后 346s 失败 = rollout status 5m 假阴性；生产实际 v0.2.0 部署成功：backend/web 1/1 Running 零重启，lair.lcc007.top HTTP 200，/healthz/ready ok
- 遗留建议：CI 里 rollout status --timeout=5m 调大到 12m（deploy-test/deploy-prod 同样）
- 63c5d00: rollout timeout 5m→12m 已 push gitlab/test（4 处）。⚠️ master 未含此修复，deploy-prod 下次发版前需 test→master 同步；origin(GitHub) 未推
## 10:48 laircli 分类 CRUD + 后端删除 bug（小白）

- laircli 新增：cat-add/cat-rename/cat-rm + categories 增强（来源列按 userId=null 判系统预置、--book 支持）。测试 83→94 项全绿，go vet/gofmt 过，go install 已更新本机
- 生产端到端实测闭环时抓到后端 bug：services/ledger.py remove_category 校验 409 后漏调 delete_category，删除返回 200 但分类不消失。已修 + test_business_api.py 回归断言钉死
- CLI 测试顺带修 harness 日期地雷：cal mock 体检硬编码 2026-09-10，运行日到达时排序断言爆，改动态"明天"
- 生产数据待决策：seed 26 类只在空库生效，生产老库仍 16 类（17-26 缺失）；需小迁移或手动 SQL 补
- 未提交清单：laircli（ledger.go/dto.go/harness_test.go/commands_test.go + 三处文档）+ backend（services/ledger.py、test_business_api.py）
## 11:35 记账分页默认 10 条 + 移动端适配（未提交，等超哥发话）

- 需求：PC 记账页默认每页 10 条（原 20）+ 移动端适配优化
- 分页：`index.vue` query 初始 + switchBook 重置 pageSize 20→10；`mock/ledger.mock.ts` fallback 20→10
- 移动端（断点对齐手机壳 860px）：
  - `LedgerTable.vue`：≤860px 隐藏 6 列表格（`class="desk-table"`），改单面板行列表（上行：分类badge+备注truncate+金额；下行：日期·记账人+编辑/删除）；m-act 按钮 44px 触控目标（负 margin 防撑行）；分页按钮 min-height 40px；panel-head 允许 wrap
  - `LedgerFilter.vue`：≤860px 日期档位横向滑动（scrollbar 隐藏）、搜索框独占整行、分类选择器弹性、控件 38px
  - `LedgerStrip.vue`：≤680px 两列 grid，预算/趋势各占整行（960px wrap 规则保留）
  - `LedgerSummary.vue`：hero 断点 760→860 + padding 收窄 14/16
  - `BookSwitcher.vue`：≤860px 下拉菜单 max-width calc(100vw-48px) 防溢出
- 验证：`pnpm run build` 通过（1.6s，类型检查零错误）
## 11:54 两批改动 commit + 双远程 push（小白）

- 0d5fe5d feat: laircli 分类管理命令 + 修复后端删除分类不生效（含 .gitignore 忽略 lair.exe；早上 10:48 遗留批）
- 6341ec4 feat: 记账页默认每页 10 条 + 移动端适配（本次批）
- push test→63c5d00..6341ec4：gitlab 与 origin(GitHub) 都推成功——GitHub 这次网络通了（此前一直推不动）
- 未提交遗留：.workbuddy/（工作区数据）与 lairweb/mock/*.mjs 临时文件，均不提交
## 22:55 .gitignore 收敛 + memory 入库（小白）

- 删掉 `lairweb/mock/auth.mock.ts.<hash>.mjs`（vite-plugin-mock-dev-server 编译产物，每次 dev 都会重新生成）
- `.gitignore` 末尾加 `.workbuddy/outputs/`（UI 迭代截图，12 张）忽略；`.workbuddy/memory/` 明确**入库**，与 notes-lab 那边的笔记规范保持一致
- 约定：`.workbuddy/memory/` 可提交，`.workbuddy/outputs/` 不提交
