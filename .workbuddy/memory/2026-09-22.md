# 2026-09-22 工作日志

## 新增：顶栏日期「上班 / 休息 / 节日」徽标（节假日 + 调休补班识别）

### 需求
超哥要求日期显示时能看出当天是上班日还是休息日。

### 调研结论（开源方案对比）
| 方案 | 代表 | 数据形态 | 判定 |
|---|---|---|---|
| 稀疏配置表（选中） | JayceChant/goliday | 每年 TOML，只写被调整日期（20~40 条） | 配置优先 → 未覆盖回退周末 |
| 全量快照 | NateScarlet/holiday-cn | `{year, papers, days[{name,date,isOffDay}]}` | 查表，但 isOffDay 布尔值把「法定节日」与「调休放假」混在一起 |
| 在线 API | timor.tech | type 枚举 0/1/2/3 | 个人公益服务，官方明确不建议用于生产核心链路 |

关键发现：holiday-cn 的 `isOffDay` 表达力不足（10/5 调休放假与 10/1 法定节日同为 true），
故采用「两手稀疏表」模型区分 `festivals` / `workdays`，而非沿用单一布尔值。

### 落地实现（纯前端，**后端零改动，无数据库表**）
- `lairweb/src/lib/holidays/types.ts`：`YearConfig` / `DayInfo` / `DayKind` 类型
- `lairweb/src/lib/holidays/2026.ts`：稀疏表，33 天放假 + 6 天调休补班 = 39 条
- `lairweb/src/lib/holidays/index.ts`：判定核心
  - `dayInfo()` 优先级：festivals → workdays → 周末 → 工作日
  - `dayBadge()` 三档徽标；`daysToNextRest()` 距下个休息日
  - **跨年**：按日期自身年份取配置（不沿用展示月份）
  - **年份缺失**：`degraded: true` 标记 + 一次性 console.warn，不静默当周末
  - **地区**：注释明确只覆盖中国大陆，港/澳/台需各自维护
- `lairweb/src/App.vue`：桌面顶栏 + 手机顶栏两处接入徽标
- `lairweb/src/style.css`：`.day-badge` 四档样式（holiday 暖橙 / rest 绿 / work 蓝 / makeup 橙描边）
- `scripts/gen-holidays.mjs`：**年更脚本**，从 holiday-cn 或本地 JSON 生成 `{year}.ts`，带校验

### 验证
- 数据表与 holiday-cn 官方 39 条**逐条交叉核对完全一致**；补班日均为周末 ✓
- vite-node 跑 15 项断言全过（含降级路径、跨年、中秋撞周末等边界）
- `pnpm run build` 类型检查 + 打包通过

### 年更流程（每年 11 月国办发文后）
```bash
node scripts/gen-holidays.mjs --year 2027 --remote
```
然后两处都要改：① 在 index.ts 的 REGISTRY 登记新年份 + 补 import；② 抽查春节/国庆调休首尾日。

### 待确认
- 日历页（calendar 模块）月历格子是否也要标徽标 —— 待超哥定，涉及 `Calendar` 组件改造

## 排错：PVE 宿主启动进 emergency mode（fstab 挂载项设备缺失）

超哥发来控制台截图求助，最终已恢复正常。

### 现象（截图）
```
Timed out waiting for device dev-disk-by\x2duuid-ccd516dc\x2e.device
[DEPEND] Dependency failed for mnt-sata256g.mount - /mnt/sata256g.
[DEPEND] Dependency failed for local-fs.target - Local File Systems.
[DEPEND] Dependency failed for systemd-fsck@dev-disk-by\x2duuid-ccd516dc...
You are in emergency mode...
```

### 定位结论
- **与 ceph 无关**：`ceph-d55d9207…`/`ceph-7f179629…`/`ceph-288bcb80…`/`pve` 四个 VG 均正常 Found 且各 1 个 LV，`/sbin/fsck.xfs: XFS file system.` 也只是正常输出。
- **根因**：`/etc/fstab` 中 `UUID=ccd516dc-…  /mnt/sata256g` 指向的块设备开机时不存在 → systemd 等满默认 90s 设备超时 → `mnt-sata256g.mount` 失败 → `local-fs.target` 失败 → 进 emergency mode。典型的「单块非关键数据盘掉线拖死整机启动」。

### 给出的处置路径
- rescue shell：`mount -o remount,rw /` → `blkid | grep -i ccd516dc` → `lsblk -f` → `journalctl -xb | grep -B5 -A20 ccd516dc`
- 三分支判因：① 盘物理掉线（`lsblk` 无盘 + `dmesg | grep -Ei 'ata|sata|scsi|error'`）② 盘在但 UUID 变更（`blkid` 对不上）③ 设备实为 LV 而所在 VG 未激活（`pvs/vgs/lvs`）
- 永久修复：UUID 变更则改 fstab；盘不再需要则注释该行；非关键盘加 `nofail,x-systemd.device-timeout=10` 防复发。
- ⚠️ **红线**：确认盘归属前禁止格式化/重建分区表，防误伤 ceph OSD 的 wal/db 设备；恢复后查 `ceph -s`、`ceph-volume lvm list` 看 OSD 是否 flapping。

### 事后 ssh pve02（192.168.6.7）实查确认
- **256G 盘确实不在系统里**：`lsblk` 只有 `sda`（119.2G INTEL SSDSC2KF128G8L，PVE 系统盘）+ 三块 NVMe —— `nvme0n1` 953.9G / `nvme1n1` 476.9G / `nvme2n1` 1.8T，**全部被 ceph OSD 的 `ceph_bluestore` LV 占满**。`blkid | grep ccd516dc` 无输出。→ 不是 UUID 变更，是盘缺失。
- `/etc/fstab` 第 6 行已被注释：`#UUID=ccd516dc-7d14-4d9e-a62b-93e3875def63 /mnt/sata256g ext4 defaults,noatime 0 2`，`/mnt/sata256g` 当前未挂载（df 只有 `pve-root`）。这就是恢复的原因。
- dmesg 无 ATA/SCSI I/O error；`sdb~sdi` 那 8 个 "SCSI removable disk" 是 USB 读卡器的多 LUN，非故障。
- 主机：pve02，pve-manager 9.1.5，kernel 7.0.14-17-pve；ceph mon/mgr/mds 均为单节点自持。

### 新发现：`k8s-rbd` 池 CRUSH 规则配错（HEALTH_WARN 的真正根因）
- `k8s-rbd`（pool 6，size 2 / min_size 1）用的是 **crush_rule 0 = `replicated_rule`**，failure domain = **host**；而集群只有 **1 个 host（pve02）** → 第 2 份副本无处安放 → **64 个 PG 全部 `active+undersized+degraded`**，健康摘要里的 0.5% degraded / 64 pgs undersized 全出自这一个池。
- 对照组：`vm_storage`(2) / `cephfs_data`(4) / `cephfs_metadata`(5) 同样 size=2，但用的是 **crush_rule 1 = `same-host`**（`choose_firstn type osd`，failure domain = **osd**）→ 268 PG 全部 active+clean。
- `single_storage`(3) size=1 + rule 0、`.mgr`(1) size=1，size=1 不受影响。
- **修复**：`ceph osd pool set k8s-rbd crush_rule same-host` → 触发 backfill 补齐第 2 副本。该池仅 2.0 GiB / 1.31k 对象，迁移很快，且可随时设回 rule 0 回滚。
- 沉淀的硬约束：**单节点 Ceph 里 size>1 的池必须挂在 osd 级故障域规则上**，默认 `replicated_rule`（host 域）在单 host 下永远 undersized。
- 次要 WARN：64 pgs not (deep-)scrubbed in time，是同一批 PG，变 clean 后会自动补 scrub。

## 调研：JuiceFS 的 4K 随机 IO 性能（超哥问「juniefs 的 4k 性能搞不搞」）

**结论：4K 性能不由 JuiceFS 本身决定，由数据命中哪一层决定，跨 4 个数量级。**

### 分层实测（4K 随机读，官方/DZone 数据）
| 层级 | IOPS | 延迟 |
|---|---|---|
| 穿透对象存储（同步 4K） | **25** | 30–55 ms |
| 穿透对象存储（libaio qd64） | 1,530 | — |
| 分布式缓存（同步 4K） | 6,893 | 0.1–0.2 ms |
| 本地缓存（同步 4K） | 14,700 | 0.05–0.1 ms |
| 本地缓存（libaio qd64） | 30,800 | — |
| 本地缓存（qd64 ×4 并发） | 116,000 | — |

### 官方 benchmark（32C/64G/40Gbps，分布式缓存供数）
- 4K randread 单进程 iodepth=8 峰值 **68K IOPS**；多进程 numjobs=12 → **258K IOPS**，延迟 ~0.4ms。
- 对照 GPFS：iodepth=16 达 83K；numjobs=12 读不同文件 433K（读同一文件因锁竞争 numjobs>2 后下滑）。
- 4K 随机写依赖 **writeback**：cache-dir 设本地 NVMe 时 numjobs 1→3 从 28K→56K IOPS；不开 writeback 直接被后端对象存储卡住。

### 途虎生产实测（JuiceFS + Ceph RADOS，关本地缓存）
- 4K 随机读 **77,984 IOPS** / 3,394 MiB/s；4K 随机写 **3,836 IOPS** / 4,120 MiB/s（MinIO 后端随机写仅 1,194 IOPS）。
- 小文件写 817 files/s（vs MinIO 147.2，**5.5×**）；小文件读 3,776 files/s（3.2×）。
- 原因：librados 直连 RADOS，比 RGW(S3) 少一层网关转发。

### 选型判断规则
- 数据集 < 缓存容量 / 有热点（AI 训练数据、模型加载、镜像层、日志）→ ✅ 能搞，接近本地盘。
- 持续 4K 随机读且数据集远大于缓存 → ❌ 别搞，落到 25–1,530 IOPS 级别。
- 数据库 / VM 镜像（fsync 敏感）→ ⚠️ 谨慎，writeback 牺牲一致性语义（数据先落本地缓存异步上传）。
- 元数据/小文件 ops → ✅ Redis 元数据引擎足够快，远胜 NFS。

### 给 pve02 场景的建议
- 后端优先 **RADOS（librados）而非 RGW/S3**；元数据引擎上 Redis；客户端 `cache-dir` 挂本地 NVMe。
- 注意单节点 Ceph 只有 3 OSD、无并行优势，后端 4K 底子有限；JuiceFS 的价值在于把 Ceph 包成多 Pod 共享文件系统。

### 避坑
- 网传「4K randrw iodepth=64 → 285K IOPS」是**缓存命中**数据，不能作容量规划依据。判断标准永远是命中率。

## 调研：JuiceFS 部署形态（自建 vs 托管，阿里云有没有托管）

- **阿里云官方没有 JuiceFS 托管产品** —— 阿里云自家的托管文件存储是 NAS / CPFS，跟 JuiceFS 是两回事。
- 但 **Juicedata 官方在阿里云云市场上架了「JuiceFS 企业版」**：`market.aliyun.com/products/9000000215/cmfw00069259.html`，交付方式=人工服务，开票主体=**杭州果汁数据科技有限公司**，2024-12-27 上架。

### 三条产品线（juicefs.io/zh-cn/pricing）
| 形态 | 元数据引擎 | 部署 | 规模上限 | 计费 |
|---|---|---|---|---|
| **社区版** | 自建 Redis/TiKV/MySQL/PG | 用户自建 | 1亿–100亿文件 / 1–100PB / 1万–100万元数据 IOPS / 100–10000 挂载 | 免费 |
| **云服务** | Juicedata 自研托管（多租户） | SaaS | 1亿文件 / 10PB / 10万 IOPS / 1000 挂载 | 按量付费 |
| **企业版** | 自研独享，可横向扩展 | 私有部署 / BYOC，100TB 起 | 5000亿文件 / 4EB / 可扩到 1000万 IOPS / 10000 挂载 | 按量计费 |

- 延迟差异关键：**云服务**元数据在 Juicedata 公有云，需公网/VPC Peering 访问，同区 **1–3 ms**；**企业版**控制台+元数据全在企业内网，**<1 ms**，随机读写场景差距明显。
- 元数据引擎是延迟的关键路径 —— 社区版的元数据延迟取决于你自建 Redis/TiKV 的位置和质量。

### 阿里云生态集成
- **FC（函数计算）Sandbox** 与 **ACS（容器计算服务）** 已把 JuiceFS 挂载能力集成进各自驱动/Sidecar，社区版与企业版都支持。
- ⚠️ **ECI / ACS 虚拟节点上 `juicefs-csi-driver` 失效**，需走 **Fluid**（ACK 云原生 AI 套件）。
- ACK 常规节点标准做法：JuiceFS CSI 驱动 + StorageClass（metaurl / storage=oss / bucket / access-key / secret-key）+ ReadWriteMany PVC。
- 阿里云 OSS 作后端是官方一等公民，权限策略需 `oss:GetObject / PutObject / HeadObject / DeleteObject`。

### 建议组合（k8s + 阿里云 OSS 已定）
**社区版自建 + 元数据用阿里云 Redis（内网）+ OSS 后端 + JuiceFS CSI**。起步最省事、数据主权完全在自己 bucket；等规模上到千万级文件 / 几百 TB、或元数据成为瓶颈，再评估云服务/企业版。

## 调研：JuiceFS 各厂商落地模式（超哥问「分享的这个案例咋落地的，各大厂商」）

### 收敛后的参考架构（各家高度一致）
应用（训练框架 / 推理服务 / Spark·Hive / Notebook）→ 接入（POSIX · HDFS API · S3 网关 · K8s CSI / Fluid）→ JuiceFS 客户端（本地 NVMe 缓存 L1）→ 控制流走元数据引擎、数据流经分布式缓存集群 L2（一致性哈希）→ 对象存储后端（OSS/COS/TOS/S3/Ceph RADOS）。

### 云厂商（做平台 / 做服务）
- **腾讯云**：JuiceFS + FoundationDB 企业级统一存储平台。**无状态元数据服务 + ZooKeeper**（服务注册/发现/状态同步），客户端走 RPC 不再直连 FDB；FoundationDB 7.3 triple 三副本 + SSD，单卷支撑 **108 亿**元数据。两种模式：托管模式（chunk/slice/block 写对象存储）+ 外部模式（UFS 抽象层，不迁数据，fingerprint 保证一致性）。两级缓存 L1 本地 + L2 自研独立缓存集群，支持预热/防击穿/TTL 驱逐/透明降级。Agent Runtime 用 Sidecar 模式挂载。
- **火山引擎（字节）边缘渲染**：选型历程 = 内部存储组件（IDC 导向、机器规格要求高）→ **CephFS + MinIO**（1000 万文件后性能退化、偶发卡顿）→ JuiceFS。**元数据用 MySQL**（单实例 / 一主二从，MySQL Operator orchestrator 做同步，高性能云盘由 Ceph 提供作数据盘），理由：千万级文件 + 读多写少场景下 MySQL 运维/可靠性/事务更优。K8s **DaemonSet 挂载 + HostPath** 给渲染 Pod；LDAP 认证；S3 网关 + POSIX 统一（替换原来 TOS 上传→下载→渲染→回传的多次拷贝）。开缓存吞吐提升 3–5 倍。日志服务 TLS 做可观测。
- **阿里云**：无自研托管；FC Sandbox（Sidecar-like 每 Sandbox 一个客户端）+ ACS（驱动集成挂载能力）；云市场售 Juicedata 企业版。

### 企业客户速查
| 客户 | 形态 | 元数据引擎 | 后端 / 亮点 |
|---|---|---|---|
| MiniMax | 企业版 | 自研（亚毫秒）+ 中心元数据分发引擎 | 镜像 FS 复制到边缘集群，边缘不存数据只预热；NVMe 全闪；百 PB |
| 阶跃星辰 | 社区版 + 企业版 | TiKV（作者是社区贡献者） | 自研对象存储；镜像 FS 部署在 200G 存储网；GPU 节点 NVMe 组分布式缓存（PB 级、单副本）；100G 主备专线 |
| 知乎 | 社区版 + 企业版 | Redis | 4 家公有云各一 K8s 集群；HDFS 存样本（Alluxio 管），JuiceFS 存 checkpoint + notebook；社区版 3.5 PB |
| 理想汽车 | 社区版 | 托管 Redis → 计划迁 TiKV | Spark ETL 直读写（HDFS API 兼容、改路径无感），日均新增 2TB；MatrixDB 时序库温冷数据下移 |
| 途虎 | 社区版 | — | **Ceph RADOS（librados）**；统一 AI 存储平台，替代 NFS/Alluxio/MinIO/SeaweedFS；小文件写 5.5× |
| 卓驭（大疆车载） | 社区版 | — | 多存储池（单卷对多 S3 Bucket）；百 PB / 百 GB/s / PB 级缓存；Fluid 主动预热；TCP 零拷贝；模型配送共享缓存使部署提速 10 倍以上 |
| 九识智能 | — | — | L4 自驾；弃 Alluxio（学习曲线陡、社区版卡死）与 CephFS（扩 OSD/rebalance 慢、小文件写差） |
| 哈啰 | — | — | 共享 Workspace + 计算 Pod 无状态；CPFS + JuiceFS + 对象存储三类并用 |
| 地瓜机器人 | — | — | 1.2 亿文件单目录；训练启动快 7×；跨云同步快 24× |
| NAVER | — | — | Hadoop → JuiceFS，写吞吐 1.7×，0 代码改动 |
| Lepton AI | 企业版 | — | 多租户低延迟云存储；成本比 EFS 低 98% |
| 携程 | — | — | 10 PB 级工程实践 |

### 五个血泪坑（实战价值最高）
1. **知乎**：Redis 单线程 + JuiceFS 对 >6.4GB 文件的 scan 操作，被 PyTorch 增量写 zip_file 触发的 truncate 激发 → **setattr 耗时 577 秒** → CPU 打满、集群不可用。
2. **理想汽车**：托管 Redis 单实例扩容瓶颈限制文件总数 → 转 TiKV。（⚠️ 该文「因需要事务 API 无法使用集群模式」的表述不准，已在下文补正 —— 官方 v1.0.0+ 支持 Redis Cluster，但 hash tag 会把单卷锁死在单节点，用了也白用。）
3. **阶跃星辰**：激进预读调优把单节点拉到 100+GB/s → 读线程过多导致缓存组节点失联、**缓存雪崩** → 结论「存储性能不必追求极致，不成为瓶颈即可」。
4. **火山引擎**：CephFS + MinIO 在 1000 万文件量级性能退化 + 偶发卡顿。
5. **九识智能**：Ceph 扩 OSD 与 rebalance 耗时、小文件写性能差。

### 提炼的规律
- **元数据引擎演进路线**：Redis（起步，受单实例限制）→ MySQL（千万级，运维友好）→ TiKV（十亿级）→ FoundationDB（百亿级）→ 企业版自研（千亿级 + 亚毫秒）。
- **缓存模式统一**：L1 本地 NVMe + L2 分布式缓存集群（一致性哈希路由）+ 预热编排（Fluid DataLoad / 自研 API）。
- **跨云统一命名空间靠「镜像文件系统」**：主 FS 在中心，边缘部署镜像，元数据自动复制，边缘不存数据只预热缓存。
- **自建场景后端**：Ceph RADOS（librados）明显优于 RGW/S3 网关（途虎实证）。

## 补正：JuiceFS 对 Redis Cluster 的支持真相（超哥追问「redis cluster 也可以这样吧」）

- ⚠️ **官方确实支持 Redis Cluster**（v1.0.0+），并非"不支持"。META-URL：`redis[s]://[user:pwd@]addr:port,addr:port,addr:port[/DB]`。
- **机制**：靠 Redis **Hash Tag** —— JuiceFS 给所有 key 加 `{DB}` 前缀（如 `{1}`），把同一文件系统的全部 key 锁进**同一个 slot**。官方中文原话：「为了避免在 Redis 集群中执行跨节点事务，**同一个文件系统的元数据总会坐落于单个 Redis 实例中**」。
- **代价**：分片只到「卷级」—— 一个卷 = 一个节点，**单卷变大只能纵向扩容**。官方 blog 原话："if there is one file system that is particularly large, scaling can only be done through vertical scaling"。
- → 所以「因需要事务 API 无法使用集群模式」表述不准，实质是**用了 Cluster 也白用**，单卷仍被锁单节点。上一条引用该说法未查证，此处更正。
- **容量公式**：每个文件元数据约占 **300 字节内存** → 1 亿文件 ≈ **30 GiB** 内存。用 `INFO memory` 的 `used_memory_dataset` 核对（不含 `used_memory_overhead`）。必须设 `maxmemory-policy noeviction`，否则元数据可能被驱逐。
- **Redis 系（含 Cluster）治不好的两点**：① 主从**异步复制**，必然可能丢元数据（丢元数据 = 文件系统损坏）；② 全内存成本高。官方建议改用带强一致的 Redis 兼容服务：阿里云 Tair、华为 GaussDB for Redis、AWS MemoryDB。
- 其他要点：`route-read=replica`（v1.0.0+，只读挂载走副本降低 master 负载）；Valkey 与 Redis 在使用上完全等价。
- **核心结论**：Redis Cluster 解决的是「多卷共用一个集群」的运维成本，**不是「单个大卷怎么扩容」的容量问题**。要单卷横向扩，只有 TiKV / FoundationDB / 企业版自研这类真分布式事务引擎。

## 补充：元数据容量公式 + 社区真实规模（官方来源）

- **容量公式**（官方文档）：键值库（Redis/TiKV）**300 字节/文件**；关系型（SQLite/MySQL/PG）**600 字节/文件**。
  放大因素：平均文件 >64MB、频繁修改产生碎片、扩展属性多、平均文件名 >50 字节。
  迁移换算例：MySQL 用量 30GB → 目标 Redis 至少备 15GB 内存。
  **实测反推系数**：按官方文档示例（`used_memory_dataset` 13.44GB ≈ 4480 万文件，而 `used_memory_rss` 20.68GB）→ **RSS ≈ 文件数 × 462 B ≈ 数据集 × 1.54**（overhead 约占 30%）。
  **1 亿文件**：数据集 30GB → 实际 RSS **约 46GB** → 生产实例按 **64GB** 规格备（留 fork/COW 余量）；**主从架构内存直接翻倍到 128GB**。这正是社区「运行中 Redis 集群最大约 1.5 亿文件」的根本原因。
  自查命令：`redis-cli INFO memory | grep -E "used_memory(_dataset|_rss|_overhead)?:"`。

## 深挖：为什么是「300 字节/文件」（超哥追问「为啥要这样算、为啥这么多」）

### 第一层放大：一个文件 ≠ 一条 KV，而是 2~4 条
JuiceFS TKV 完整 key 表（源码 `pkg/meta/tkv.go`）：

```
setting                          format
C{name}                          counter (nextInode/nextChunk/nextSession)
A{8B-inode}I                     inode 属性（权限/时间/大小/nlink）
A{8B-inode}D{name}               dentry 目录项
A{8B-inode}P{8B-inode}           parents（硬链接）
A{8B-inode}C{4B-blockID}         file chunks（每 64MB chunk 一条）
A{8B-inode}S                     symlink target
A{8B-inode}X{name}               extended attribute
D{8B-inode}{8B-len}              deleted inodes
F{8B-inode} / P{8B-inode}        Flocks / POSIX locks
K{8B-sliceID}{8B-blockID}        slice refs（引用计数）
L{8B-sliceID}                    delayed slices
SE/SH/SI/SS{8B-sessionID}        session 相关
U{8B-inode}                      目录用量统计
N{8B-inode}                      detached inode
QD{8B-inode}                     目录配额
R{4B-aclID}                      POSIX acl
```

- **设计巧思**：全部用 `A` 前缀 + 8 字节 inode，使**同一 inode 的所有 key 前缀相同** → 落在同一 Region/slot，把事务尽量收敛到单节点（对应前面说的「第三个 key 要靠近前两个」）。
- 编码：inode/counter 用小端 8 字节；sessionID/sliceID/timestamp 用大端 8 字节。
- 普通文件最少 **2 条**（inode 属性 + dentry）；带数据再加 `C{blockID}`；每个 slice 再加 `K{sliceID}*`。

### 第二层放大：Redis 每条 KV 的固定开销 ≈ 88~112 字节
| 组成 | 字节 |
|---|---|
| dictEntry 结构 24B → jemalloc 分配 | **32B** |
| redisObject | **16B** |
| key SDS（4B 头 + 内容，jemalloc 取整） | ~16B |
| value SDS | ~16B |
| 哈希桶指针（桶数组 ≈ key 数 × 8B） | **8B** |

- ⚠️ **jemalloc size class 阶梯效应**：key/value 从 12 字节增到 13 字节，SDS 由 16 跳到 32 → **单条 dictEntry 直接从 80B 变 112B**。字符串长度微调 1 字节，内存可能翻倍。

### 结论算式
`2~4 条 × ~100 字节 ≈ 300 字节/文件`；关系型数据库多一层表索引 → `600 字节/文件`。

### 实践启示（重要）
**文件数本身就是元数据成本**，所以把小文件打包成大文件（tar / parquet / webdataset）能直接把元数据内存需求降一个数量级。阶跃星辰也提到：打包后的数据集面临高并发随机读，未打包则大量小文件直接冲击元数据性能。

## 审阅 notes-lab 的 JuiceFS 笔记（超哥：「你看我的 juicefs 里面」）

- 目标文件：`D:\001-code\笔记\存储\JuiceFS\JuiceFS-简介.md`（2026-09-07，10875 B，`存储/JuiceFS/` 下唯一一篇；库规范见 `存储/AGENTS.md`）
- 对照本轮调研，发现 6 处待补/待修：

| # | 位置 | 问题 | 性质 |
|---|---|---|---|
| 1 | 元数据引擎选型表 | 把「**TiKV / etcd**」并列，适用写「海量文件（十亿+）」 | ❌ **实质性错误**：etcd 全量复制不分片、单 Raft Group、官方建议 ≤8 GB，不可能撑十亿文件，必须拆开 |
| 2 | 坑·Redis 吃内存 | 只写「实测量级约数百 B/文件」 | ⚠️ 太模糊，无法做容量规划 → 应补精确公式（KV 300B/文件、关系型 600B/文件、RSS×1.54、四个放大因素） |
| 3 | 元数据引擎选型表 | 缺「容量上限（文件数）」这个决定维度 | ⚠️ 应补：Redis 1 亿推荐 / 1.5 亿实测天花板；TiKV 10 亿+（最大 70 亿 / 15 PiB） |
| 4 | 选型/坑 | 未提 Redis Cluster 真相 | ⚠️ 常见误区无答案：支持（v1.0.0+）但 hash tag 把单卷锁单节点，**加节点不解决单卷扩容** |
| 5 | 全文 | 无「300 字节从哪来」的解释 | 信息缺口：TKV key 结构（一个文件 2~4 条）+ Redis 单条 KV 开销 88~112 B |
| 6 | 选型表 | 「（及兼容变体，需事务支持）」一笔带过 | 可选：展开 Tair 形态差异与「强一致」的真相 |

- 拟落笔方案：**并入 `JuiceFS-简介.md`，新增一节「元数据引擎：选型与容量规划」**（遵守超哥「能并入已有主笔记就不新建」的硬规矩，不新开文件）。待超哥确认后写入。

### 落笔完成（超哥确认「改下，还有其他的笔记也要改下」）

已改 5 个文件，共 +98 / -15：

1. **`存储/JuiceFS/JuiceFS-简介.md`（+96/-13）**
   - frontmatter：description 补「元数据引擎选型与容量规划」、tags 加「元数据引擎」、`timestamp: 2026-09-07` → `2026-09-22`（核对 log.md 确认 timestamp 语义为**最后修订日期**，随修订更新）
   - 把「### 元数据引擎选型」扩写为 `### 元数据引擎：选型与容量规划`，四个子节：
     - **3.1 硬约束**：必须支持事务（rename 要改多条 key，可能跨分片）
     - **3.2 引擎对比**：❌ **拆开原文错误的「TiKV / etcd」并列**，补「容量上限（文件数）」列 + etcd vs TiKV 对照表
     - **3.3 容量计算公式**：300B（KV）/ 600B（关系型）、2~4 条 key 的拆解、单条 KV 88~112B 开销、RSS×1.54、1 亿文件→64GB 规格/主从 128GB、jemalloc 阶跃坑、跨引擎迁移 1/2 换算、小文件打包降 1000 倍、自查命令
     - **3.4 两个陷阱**：Redis Cluster 伪分片（hash tag 锁单卷）、主从异步丢账本（含 Tair 持久内存型的细节与 Lua 风险）
   - 第五节「坑」里模糊的「实测量级约数百 B/文件」→ 改为引用新章节的精确数字
2. **登记同步**：`存储/存储.md`、`存储/AGENTS.md`（结构树 + 快速查找表 2 处）、`index.md` —— JuiceFS 条目描述补「元数据引擎选型与容量规划」
3. **`数据库.md/Redis/05 - 主从、哨兵、集群.md`（+9）**：4.3 原理补「**两个硬限制**」——① 多 key 操作与事务不能跨 slot（`CROSSSLOT` 报错）② hash tag 是唯一绕行手段但有存储倾斜代价；并交叉引用 `[[存储/JuiceFS/JuiceFS-简介#元数据引擎：选型与容量规划]]`

**检查过但不需改**：`AI/AI平台分层架构.md`（⑦存储层仅列表提及 JuiceFS）、`工作.md/面试经验/*`（岗位 JD 提及 JuiceFS/Fluid；面试复习手册里的 etcd 全是 K8s 语境）、`容器.md/K8S/*`（etcd 均为 K8s 语境）、Redis/04 与 07。
**全库 `TiKV` 只出现在 `JuiceFS-简介.md` 一篇**，说明 etcd/TiKV 并列的错误没有扩散。

**log.md 未改**：按根 `AGENTS.md`「仅在准备 Git 提交前更新」，等超哥发话 commit 时再统一汇总条目。
**未提交**（超哥未发话）；注意仓库另有遗留未提交改动（`IPMI.md`、`.workbuddy/memory/MEMORY.md`、`2026-09-20.md`），别误 add。

## HDFS 元数据容量公式（超哥问「hdfs 的元数据放到哪个 node」）

- 答：**NameNode**（元数据节点）。四角色：NameNode（元数据，**全量驻内存**）/ DataNode（数据块）/ SecondaryNameNode（只做 fsimage+edits 的 checkpoint，**不是热备**）/ JournalNode（HA 下两个 NN 共享 edits log，QJM）。
- NameNode 元数据两形态：内存 = Namespace + BlocksMap；磁盘 = `fsimage`（快照）+ `edits`（增量日志，启动回放）。
- **Cloudera 官方公式**：`NameNode 内存 = (文件数 + 块数) × 150 字节`——**150 B 是按「对象」算**（1 个 inode 或 1 个 block 各算一个对象），不是按文件。落地值：**每 100 万 block 配 1 GB 堆**（含 RPC/bookkeeping 开销）。
- ⚠️ **副本数不影响 NameNode 内存**，只影响磁盘（3 副本不占 3 份内存）。
- 经典对照（同为 1 GB 数据）：1×1024MB 文件 = 9 对象 = 1,350 B；8×128MB = 16 对象 = 2,400 B；**1024×1MB 小文件 = 2,048 对象 = 307,200 B**（差 227 倍）——这就是 HDFS 不适合小文件的根子，与前面「打包小文件降元数据成本」同一道理。
- 与 JuiceFS 对照：HDFS 按对象 150 B，JuiceFS 按文件 300 B（约 2 个对象），**量级一致**——两者都是「元数据全驻内存 + 按文件数吃内存」，区别在扩展性（HDFS 靠 Federation 切目录，JuiceFS 换 TiKV 即可单卷水平扩）。

## HDFS NameNode 瓶颈的官方解法（超哥追问「namenode 是不是也遇到这个情况，官方有什么方案」）

- **官方推荐上限 ≈ 3 亿文件（300 million）**（Cloudera）；实践中 NameNode 堆 >100 GB 后 GC 停顿变长、RPC 队列堆积，运维只能靠重启 NN 应对。
- **三层解法**：

  1. **横向切分（兼容 HDFS，不动数据）**
     - **Federation**（Hadoop 2.0+）：多 NN，各自独立 namespace（按目录切）+ 独立 block pool；**DataNode 共享**（同时参与所有 block pool，分别心跳与块汇报）。内存线性扩；代价是要规划目录归属、客户端本地配 ViewFs 挂载表。
     - **Router-Based Federation / RBF**（Hadoop 3.0+）：加一层**无状态 Router** 提供统一视图，Router 查 State Store（ZK/etcd）的 Mount Table 后把请求代理到正确 NN。**Router 不在数据路径上**（拿到 block map 后客户端直连 DataNode）。官方文档点名 NN 扩展受限的三个来源：inodes+blocks 元数据开销、DataNode 心跳数、RPC 请求数。
  2. **减少文件数 / 块数（工程手段）**
     - **HAR（Hadoop Archives）**：`hadoop archive -archiveName x.har -p /dir /out`，用 `har://` scheme 透明访问，**不压缩**（类比 tar）。Hive 内置 `ALTER TABLE ... ARCHIVE PARTITION`，能把 100 个文件的分区变成 ~3 个。**代价：查询变慢、失去数据本地性**；官方 cwiki 标注为 advanced command 且有未修 bug（HarFileSystem 无 locality）。
     - SequenceFile（key=文件名 / value=内容）、CombineFileInputFormat（计算侧合并 split）
     - 调大 block size（对真小文件无效）、名称配额 + 空间配额
  3. **换架构：Apache Ozone（官方根本解法）**
     - 元数据**存 RocksDB 落磁盘**，不占堆内存；namespace 与 block 管理拆成 **OM / SCM** 两个可独立扩展的进程；用 **container 报告替代 block 报告**（1 亿 block → 2121 个 5 GB container，报告量少 ~40 倍）
     - 官方实测（Ozone 0.5.0）：**10 亿个 10KB 小文件**，1 OM + 1 SCM + 9 DN，元数据仅占 **127 GB SSD**，OM 全程 ~12 ms 延迟、无长 GC
     - **生产案例滴滴**：数百 PB / 数百亿文件，HDFS → Ozone 已跑 2 年+；P90 GetMetaLatency **90 ms → 17 ms**，读吞吐 +20%，EC 6-3 把存储开销从 3.0x 降到 1.5x（省数百 PB）；**应用团队不再需要做小文件合并**；单集群约 50 亿文件
- **洞察**：Ozone 与 JuiceFS 是**同一思路**——把元数据从内存搬到磁盘 KV（RocksDB）以解开内存天花板。区别：Ozone 元数据自研（Ratis + RocksDB）且数据也在 Ozone 内（container）；JuiceFS 元数据外包给 Redis/TiKV、数据外包给对象存储。
- 面试口径：小文件答「每文件/块各占约 150 B NN 堆内存 → namespace 对象数暴涨 → NN 是单点，内存即天花板」；解法按「Federation/RBF 切分 → HAR/合并减量 → Ozone 换架构」三层答。

### 换算：滴滴规模下平均文件多大（超哥追问）

- 数百 PB ÷ 数十~数百亿文件：只要两边同量级，倍率抵消 → `10^15 B ÷ 10^8 = 10^7 B`，**平均约 10 MB/文件**（区间约 3~45 MB，中值 ~10 MB）。
- 换算成 HDFS 就是灾难：每文件 ~300 B（1 inode + 1 block）→ **500 亿文件需 15 TB NameNode 堆内存**（100 亿也要 3 TB）。而 Cloudera 官方推荐上限是 **3 亿文件（约 90 GB 堆）**，滴滴规模是它的 **167 倍**——HDFS 在物理上就不可能，这是必须迁 Ozone 的量化理由。
- 换算成 Ozone：按官方实测 10 亿文件 / 127 GB SSD（≈ **127 B/文件**，且落磁盘可分布式）→ 500 亿文件约 **6.4 TB SSD**，再用 ViewFs 把每集群压到 50 亿以下（每集群约 640 GB）。**从「15 TB 内存」变成「几 TB 可分布的 SSD」**，这才是能落地的架构。
- 注意：平均值会掩盖长尾（少数超大文件 + 海量小文件），真实分布通常更极端。

### 集群容量心算（超哥给的配置：100 台 / 256 GB 内存 / 96 核 / 36 盘位 / 16 TB）

- 裸容量 = 100 × 36 × 16 = **57,600 TB = 57.6 PB**；HDFS 三副本后 **19.2 PB**；扣 DataNode 保留（默认 10%）→ **约 17.3 PB 可写**。
- **真正的约束是 NameNode 内存，不是 TB**（19.2 PB 填满时）：

  | 文件画像 | 文件/块数 | 官方口径堆需求（1 GB/百万 block） |
  |---|---|---|
  | 128 MB | 1.5 亿 | 150 GB ✅ |
  | 10 MB | **19.2 亿** | **1.9 TB** ❌ |
  | 1 MB | 192 亿 | 19.2 TB ❌❌ |

  文件大小差 128 倍 → NameNode 内存需求差 128 倍。
- 256 GB 内存的 NN 堆上限约 **100~128 GB**（超 100 GB GC 停顿失控）→ 约 1.28 亿 block ≈ 8500 万文件，与 Cloudera「3 亿文件上限」量级吻合。
- **结论**：要单 NN 填满 57.6 PB，文件平均须 ≥ 128 MB；10 MB 级别必须 Federation/RBF 或上 Ozone。
- 工程提醒：block size 128 → 256 MB 可让 block 数直接减半（NN 内存省一半，但小文件不受益）；3600 块盘按 AFR 1~2% 平均 **每 1~3 天坏一块盘**，日常运维就是换盘 + 平衡。

### 结论固化：HDFS 的天花板是「文件数」，不是「PB 数」

超哥总结「HDFS 上线也就是 PB 级别了，再大要考虑换东西」——**一半对，需要精确化**：

- 官方上限是 **3 亿文件**，**PB 数只是文件大小的函数**：

  | 文件大小 | 3 亿文件对应总容量 |
  |---|---|
  | 1 MB | **300 TB** |
  | 10 MB | **3 PB** |
  | 128 MB | **38 PB** |
  | 256 MB | **77 PB** |

  **同一个文件数上限，容量差 256 倍。** 所以"HDFS 只能到 PB 级"不准确：大文件场景能撑几十 PB，小文件场景几百 TB 就到顶。**真正的触发条件是文件数摸到 3 亿，不是数据量到了多少 PB。**
- **三个杠杆（代价递增）**：① block size 128→256 MB（对大文件直接把上限翻倍，免费）；② **Federation/RBF**（N × 3 亿，**仍然还是 HDFS，不算"换东西"**，几十 PB 的集群普遍这么做）；③ **Ozone**（换架构，单集群约 50 亿文件）。
- **各系统文件数上限对照**：HDFS 单 NN **3 亿** | JuiceFS + Redis **1.5 亿** | JuiceFS + TiKV **100 亿+**（实测 70 亿文件 / 15 PiB）| Ozone 单集群 **约 50 亿**、可扩到数十亿级。
- **"换东西"的真实时机**：不是"到了 PB 就换"，而是 **Federation 带来的运维复杂度（多 namespace 管理 + 跨 namespace 数据搬迁）超过换架构成本时**。反过来，HDFS 在「大文件 + 超大容量」上比 JuiceFS 更强（元数据在内存、块级存储效率高）——两者是不同维度的强者。

### HDFS 单集群规模的「官方口径」与真实天花板（超哥追问「官方推荐最大多少 PB」）

- ⚠️ **Apache 官方并未给出「单集群最大 PB 数」推荐值**——因为它不是正确的度量维度。官方只声明设计目标「可靠运行在成千上万台普通服务器上」。
- **可引用的工程口径（Cloudera / Hortonworks 文档）**：
  - NameNode 堆：**每 1,000,000 block 加 1 GB**（最低 1 GB）
  - **单 DataNode 不超过 100 TB**；**单盘不超过 8 TB**（不支持更大盘）
  - 盘必须 **JBOD，禁止 RAID / LVM / ZFS**
  - 文件数上限 **3 亿**
- **HDFS 论文里的经验法则**：**「一千个节点 ≈ 1 PB 应用存储」**（one thousand nodes represent one PB of application storage）。
- **真实生产天花板**：
  - **Yahoo 单集群**：4000 节点 = **11 PB 裸 / 3.7 PB 可用**（三副本），**6500 万文件、8000 万 block**，NameNode 配 64 GB RAM
  - **Facebook 单集群**：2012 年已超 **100 PB、1 亿+ 文件**（数千节点）——已是极限，后来转自研 Tectonic
  - **Yahoo 全部 600 PB 分散在约 20 个 YARN 集群**（43000 台服务器）——**巨头也不靠单集群堆容量，而是多集群**
- **堆 → 容量换算**（Cloudera 口径，128 MB block）：64 GB 堆 ≈ 6400 万 block ≈ **8 PB**；128 GB ≈ 1.28 亿 block ≈ **16 PB**；256 GB ≈ **33 PB**。所以「单集群几十 PB」需要 200+ GB NameNode 堆，已是极限。
- ⚠️ **超哥给的配置（36 盘 × 16 TB = 576 TB/节点）远超 Cloudera 支持上限**（单 DN ≤100 TB、单盘 ≤8 TB）：物理上能跑，但**单节点故障后 576 TB 的重建时间极长**（超密集 DN 会显著拉长 machine/rack 级故障恢复），且商业版（CDH/HDP）会拒绝支持。**这种"少而巨"的节点更适合对象存储（Ceph/MinIO），HDFS 偏爱"多而小"的节点。**
- **社区真实规模**（官方 Medium 答疑）：
  - Redis 仍是社区第一大选择（一半以上），TiKV 与 MySQL 次之且在增长
  - **运行中的 Redis 集群最大文件数约 1.5 亿**，运行相对稳定；1 亿是推荐值而非硬上限
  - **TiKV 被文件总数排名前三的集群使用，均超 10 亿文件**；目前最大文件系统 **70+ 亿文件 / 15+ PiB**；官方内部实测 TiKV 存 **100 亿文件**
- **官方选型口径**：① **存关键数据的话 Redis 不合适**（会丢数据/短暂中断）；② 无论用哪个引擎都**基本需要高速 SSD**；③ 「**我们一般建议用户一开始选择自己熟悉的数据库**」；④ 元数据引擎迁移只能迁到空库。

## Tair（阿里云）与 Redis 的区别

- Tair 是**产品线**不是单品：`Redis 开源版` / `Tair（企业版）` / `Tair Serverless KV` 三类实例；企业版又分 **内存型 / 持久内存型 / 磁盘型（ESSD/SSD）**。
- 三形态官方数据：

  | 形态 | 性能（Redis 开源版=100%） | 成本 | 落盘一致性 | 持久化级别 |
  |---|---|---|---|---|
  | 内存型 | 300% | 117% | Write Back | 秒级 |
  | 持久内存型 | 90% | 70% | **Write Through** | **命令级** |
  | 磁盘型 | 读 40–60% / 写 30–40% | **15–20%** | **Write Through** | **命令级** |

- ⚠️ **关键勘误**：阿里云特性对比表中「**主备复制一致性**」一栏，**所有形态（含 Tair 全部形态与 Redis 开源版）都是「最终一致」**。真正变强的只是「落盘一致性」→ 持久内存型/磁盘型为 Write Through + 命令级持久化，解决「掉电丢数据」，但**主备切换仍有数据回退窗口**。「半同步模式」仅内存型与磁盘型(SSD)支持。
  → 上一条「官方建议用 Tair 提供强一致」表述过于笼统，应细化为「**持久化可靠性增强**」，主备同步语义未变。
- ⚠️ **对 JuiceFS 的两个风险点**：
  1. **Lua 脚本**：Tair 官方《开发运维规范》明确「推荐使用扩展数据结构，**避免使用 Lua 脚本**」「Lua 无法被多线程加速」。而 JuiceFS 元数据操作重度依赖 Lua（EVAL）→ **必须实测**；另有沙箱限制（KEYS、TIME、RANDOMKEY 等非确定性命令可能直接报错）。
  2. **CROSSSLOT + 倾斜**：Tair 集群版事务要求所有 key 在同一分片，JuiceFS 的 `{DB}` hash tag 正好满足，但官方规范也提醒「避免 hash tag 带来的存储倾斜」。
- 其他：Tair 支持 **256 个 DB**（0–255，默认 db0；Redis 开源版只有 16 个）；兼容 Redis 4.0/5.0/6.0/7.0。

## 收尾：节假日徽标功能已推送到 test 分支（origin + gitlab）

- 两个 commit：`60c2410` feat(web) 顶栏日期徽标 + `d63e657` chore(memory) 记忆记录
- 双远程均已推送，本地 / `origin/test` / `gitlab/test` 三指针同为 `d63e657`
- 未纳入提交：`.design/`、`.zcode/`（既有本地产物，与本次功能无关）

## 续：日历页月历格子也接入节假日 / 调休徽标

超哥确认「肯定要搞」，落地 `lairweb/src/modules/calendar/index.vue`。

### 关键前置发现（省掉一次返工）
读 `node_modules/reka-ui/dist/date/calendar.js` 的 `createMonth()` 源码确认：
**reka 月网格包含相邻月日期**（`lastMonthDays` + `nextMonthDays` 拼接），
不是补 null。所以必须处理「相邻月淡化」，否则跨月格子徽标会和当月抢视线。
→ 用 `date.month !== month.month || date.year !== month.year` 判定，徽标给 `opacity: .45`。

### 实现
- `cellBadge()` 三档：法定节假日 → 节日短名 / 调休补班 → `班` / 普通休息日 → `休`；
  **普通工作日不显示**（否则满屏噪音，违背项目「克制即奢侈」的设计语言）
- `shortFestival()`：>2 字去掉末尾「节」→ 清明/劳动/端午/中秋/国庆；2 字原样 → 元旦/春节。
  7 个节日名统一收敛为 2 字
- 双文案 + CSS 媒体查询切换：桌面显示 2 字节日名（`badge-full`），
  移动端切单字（`badge-mini`：假/休/班）—— 避免引入 JS resize 监听
- `badgeCache`（Map，键 = 'YYYY-MM-DD'）缓存结果，模板里 3 次调用只算一次
- 配色与顶栏徽标一致：holiday 暖橙 / rest 绿 / makeup 橙 + `inset` 描边

### 移动端尺寸核算（避免挤压，实测 CSS 推的）
`.m-content` 水平内距 14px → 日历根 `p-3` 12px → 格子 padding 2px
→ 375px 视口下每格可用 **≈42px**。原参数（26px 触发块 + 3px 间距 + 2 字徽标 ≈46px）**会溢出**，
收紧为：触发块 24px、间距 2px、徽标 `0.5rem` + `padding 0 3px` → **≈40px**，留 2px 余量。

### 验证
- 渲染 9/10 月整月网格，肉眼核对：
  - 9 月：5休 … 20班 21… 25中秋 26中秋 27中秋（9/26 周六叠加中秋 → 节日优先）
  - 10 月：1国庆…7国庆 10班 11休 …（10/4 周日叠加国庆 → 节日优先）
- 10 项断言全过；跨年（12 月网格含 2027-01）降级路径正常
- `pnpm run build` 通过；模板里的非空断言 `!` vue-tsc 接受，无需拆子组件
- 核对 dist CSS：徽标样式与媒体查询均正确带 `data-v-` 作用域，未被 tree-shake

### 备注
- `calendar/index.vue` 483 行（含 ~200 行 scoped 样式），超出 frontend-guide 提到的 300 行，
  但 assistant(591) / ledger(515) 同样超，属项目既有常态，未做拆分
- 观察：`shortFestival` 是日历专用的展示剪裁，未上提到 `lib/holidays`；
  若后续 days 模块也要短名，再考虑提取
